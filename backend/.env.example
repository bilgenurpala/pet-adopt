DATABASE_URL=postgresql+psycopg2://kullanici_adi:sifre@localhost:5432/petadopt

# JWT
# SECRET_KEY'i kendin uret, ornekteki degeri KULLANMA:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
